<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="initial-scale=1" />
  <%#
    Configure default and controller-, and view-specific titles in
    config/locales/en.yml. For more see:
    https://github.com/calebthompson/title#usage
  %>
  <title><%= title %></title>
  <%= stylesheet_link_tag :application, media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <div id="main">

    <div id="nav_bar">
      <ul>
      <li id="logo">
        <%= image_tag "/ocs_star.png", alt: "OCS Outcomes Tracker" %>
      </li>
        <% if current_user %>
            <li><%= image_tag current_user.image_url, alt: current_user.name %></li>
            <li><%= link_to 'Log Out', logout_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Login with GitHub', '/auth/github' %></li>
        <% end %>
      </ul>
    </div>

    <div id="profile_bar">
      <ul>
        <% if current_user %>
            <li><%= image_tag current_user.image_url, alt: current_user.name %></li>
            <li><%= current_user.name %></li>
            <li><%= current_user.cohort.name %></li>
        <% end %>
      </ul>
      <div id="fill"></div>
    </div>

    <div id="page_content">
      <%= render "flashes" -%>
      <%= yield %>
    </div>
  </div>
  <%= render "javascript" %>
  <%= render "css_overrides" %>
</body>
</html>
