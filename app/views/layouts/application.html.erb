<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="initial-scale=1" />
  <%#
    Configure default and controller-, and view-specific titles in
    config/locales/en.yml. For more see:
    https://github.com/calebthompson/title#usage
  %>
  <title>Outcomes Tracker</title>
  <%= stylesheet_link_tag :application, media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <div class="container">
    <%= render "refills/navigation" %>
  </div>

  <div class="main">
    <div class="container">
      <% if current_user && content_for(:sidebar) %>
        <div class="sidebar">
          <%= content_for(:sidebar) %>
        </div>
      <% end %>

      <div class="pageContent <%= "pageContent--fullWidth" if !content_for(:sidebar) %>">
        <%= render "flashes" -%>
        <%= yield %>  
      </div>
    </div>
  </div>

  <%= render "javascript" %>
  <%= render "css_overrides" %>
</body>
</html>
