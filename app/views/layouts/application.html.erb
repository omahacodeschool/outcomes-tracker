<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="initial-scale=1" />
  <%#
    Configure default and controller-, and view-specific titles in
    config/locales/en.yml. For more see:
    https://github.com/calebthompson/title#usage
  %>
  <title><%= title %></title>
  <%= stylesheet_link_tag :application, media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <div id="main">

    <div id="nav_bar">
      <ul>
      <li id="logo">
        <%= link_to( image_tag("/ocs_star.png", alt: "OCS Outcomes Tracker"), root_path, alt: 'Outcomes Tracker Home') %>
      </li>
        <% if current_user %>
            <li><%= image_tag current_user.image_url, alt: current_user.name %></li>
            <li><%= link_to 'Log Out', logout_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Login with GitHub', '/auth/github' %></li>
        <% end %>
      </ul>
    </div>

    <% if current_user %>
      <div id="profile_bar">
        <ul>
          <li><%= image_tag current_user.image_url, alt: current_user.name %></li>
          <li><%= link_to current_user.name, user_path(current_user.id) %></li>
          <% if current_user.profile && current_user.has_view_permission == false %>
            <li><%= current_user.cohort.name %></li>
            <!-- entries routing no longer exists, but keep this as a template for stats on job apps, offers, etc: -->
            <!-- <li><%# link_to current_user.entries.count.to_s + ' Entries', entries_path %></li> -->
          <% end %>
        </ul>
        <div id="fill"></div>
      </div>
    <% end %>

    <div id="page_content">
      <%= render "flashes" -%>
      <%= yield %>
    </div>
  </div>
  <%= render "javascript" %>
  <%= render "css_overrides" %>
</body>
</html>
