<p id="notice"><%= notice %></p>

<!-- <h1>Listing Entries</h1> -->
<% if current_user.has_view_permission %>
  <h1>User Activity</h1>
<% else %>
  <h1>Entries</h1>
<% end %>

<table>
  <!-- <thead>
    <tr>
      
      <th>User</th>
      <th>Company</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <% if current_user != User.find(entry.user_id) %>
          <td><%= link_to User.find(entry.user_id).name, user_path(entry.user_id) %></td>
        <% end %>
        <td><%= entry.company %></td>
        <td><%= entry.return_associated_info %></td>
        <%# if entry.job_application && entry.job_application.job_title.blank? == false %>
          <!-- <td>Application,</td> -->
        <%# end %>
        <%# if entry.offer && entry.offer.job_title.blank? == false %>
          <!-- <td>Offer</td> -->
        <%# end %>
        <td><%= link_to 'Show', entry %></td>
        <!-- <td><%# link_to 'Edit', edit_entry_path(entry) %></td>
        <td><%# link_to 'Destroy', entry, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%# link_to 'New Entry', new_entry_path %>
