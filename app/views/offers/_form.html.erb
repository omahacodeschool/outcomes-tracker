<%= form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%# f.fields_for :entry do |ff| %>
    <!-- <div class="field"> -->
      <%# f.label :user_id %><!-- <br> -->
      <%# ff.hidden_field :user_id, :value => current_user.id %>
    <!-- </div> -->
    <!-- Do we even want this to be a text field that folks can inadvertently change? Probably not. Will leave it for now. -->
    <!-- <div class="field"> -->
      <%# ff.label :company %><!-- <br> -->
      <%# ff.text_field :company %>
    <!-- </div><br><br> -->
  <%# end %> 

  <% if params[:action] == "new" %>
    <div class="field">
      <%= f.label "For which existing application?" %>
      <%= f.select(:entry_id, @entries.collect {|e| [e.company, e.id]}) %><br>
    </div>
  <% elsif params[:action] == "add_new_to_existing_entry" %>
    <div class="field">
      <%= f.label :entry_id %>
      <%= ff.hidden_field :entry_id, :value => @offer.entry_id %>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_title %><br>
    <%= f.text_field :job_title %>
  </div>
  <div class="field">
    <%= f.label :salary %><br>
    <%= f.text_field :salary %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label "Employment type" %><br>
    <%= f.select(:nature_of_employment, Offer.nature_of_employments.keys) %>
  </div>
  <div class="field">
    <%= f.label :remote %><br>
    <%= f.check_box :remote %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
