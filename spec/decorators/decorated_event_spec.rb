require 'spec_helper'

describe DecoratedEvent do
  let(:decorated_event) { FactoryGirl.build_stubbed(:decorated_event) }

  it 'formats notes' do
    # expect(book.display_days).to eq('600')
  end

  it 'formats event created time' do
    # expect(book.display_days).to eq('600')
  end

end